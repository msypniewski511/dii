<%= form_with(model: brand) do |form| %>
  <% if brand.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(brand.errors.count, "error") %> prohibited this brand from being saved:</h2>

      <ul>
        <% brand.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :business_name, style: "display: block" %>
    <%= form.text_field :business_name %>
  </div>

  <div>
    <%= form.label :website_domain, style: "display: block" %>
    <%= form.text_field :website_domain %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
